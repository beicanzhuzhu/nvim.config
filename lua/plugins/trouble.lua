return {
    "folke/trouble.nvim",
    opts = {
        focus = true,
        win = { type = "split", }
    }, -- for default options, refer to the configuration section for custom setup.
    cmd = "Trouble",
}
